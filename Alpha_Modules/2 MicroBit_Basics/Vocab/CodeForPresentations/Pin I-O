let foo0 = 0
let foo1 = 1
let foo2 = 2

input.onPinPressed(TouchPin.P0, function () {
    basic.showNumber(foo0)
})

input.onPinPressed(TouchPin.P1, function () {
    basic.showNumber(foo1)
})

input.onPinPressed(TouchPin.P2, function () {
    basic.showNumber(foo2)
})
